<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền truy cập mạng -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashCardNNN"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Màn hình chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các màn hình cho phần Quiz -->
        <activity
            android:name=".QuizSetupActivity"
            android:exported="false"
            android:label="Chọn Quiz" />
        <activity
            android:name=".QuizActivity"
            android:exported="false"
            android:label="Quiz" />
        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:label="Kết quả" />

        <!-- Các màn hình cho phần CRUD từ vựng -->
        <activity
            android:name=".VocabularyManagementActivity"
            android:exported="false"
            android:label="Quản lý từ vựng" />
        <activity
            android:name=".AllWordsActivity"
            android:exported="false"
            android:label="Tất cả từ" />
        <activity
            android:name=".TopicWordsActivity"
            android:exported="false"
            android:label="Từ theo chủ đề" />
        <activity
            android:name=".AddEditTopicActivity"
            android:exported="false"
            android:label="Chỉnh sửa chủ đề" />
        <activity
            android:name=".AddEditWordActivity"
            android:exported="false"
            android:label="Chỉnh sửa từ vựng" />

    </application>

</manifest>
